-- nvim-lspconfig: shipped for its collection of server configurations.
-- Servers themselves are enabled in `lua/svim/lsp.lua` and configured under
-- `after/lsp/`, so nothing extra is needed here.
---@type LazyPluginSpec
return {
  'neovim/nvim-lspconfig',
}
